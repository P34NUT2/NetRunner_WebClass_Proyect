# Database URL para Docker (usa nombre del servicio postgres en lugar de localhost)
DATABASE_URL="postgresql://webdev:InsecurePasswd@postgres:5432/netrunner"

# Server Configuration
PORT=3001

# Frontend URL para CORS (peticiones vienen del navegador en localhost)
FRONTEND_URL="http://localhost:3000"

# JWT Configuration
JWT_SECRET="SuperMegaInsecurepasswd"
JWT_EXPIRES_IN="7d"

# Ollama Configuration (usa nombre del servicio ollama en lugar de localhost)
OLLAMA_HOST="http://ollama:11434"

# Node Environment
NODE_ENV="production"
